name: 🧐 Spell checker (Grammar)

on:
 push:
    branches:
      - main
      - develop
 pull_request:

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: matheus23/md-spellcheck-action@v4.2.2
        with:
          files-to-check: "./src/**/*.md"
          # TODO: Create words to ignore list
          # words-to-ignore-file: ./words-to-ignore.txt
